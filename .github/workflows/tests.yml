name: Comprehensive Tests
on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]
  workflow_dispatch:  # Permite execução manual

jobs:
  structural-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
      
      - name: Setup CI environment
        run: |
          cd backend
          cp .env.ci .env
      
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      
      - name: Run structural tests (safe)
        run: |
          cd backend
          python ci_test.py
      
      - name: Run coverage tests
        run: |
          cd backend
          pytest tests/testGenertion.py::test_root tests/testGenertion.py::test_health_check tests/testGenertion.py::test_invalid_endpoints tests/testGenertion.py::test_malformed_requests tests/test_integration.py::TestPerformance --cov=app --cov-report=term-missing
      
      - name: Code quality check
        run: |
          cd backend
          python -m py_compile app/*.py
          echo "✅ Code quality check passed"
